google-re2
pyxurls
gdown==5.1.0
urlobject
huggingface_hub[cli]>=0.23,<1
di-toolkit
click
pandas
tabulate
pyquery
pyrfc6266
git+https://github.com/deepghs/waifuc.git@main#egg=waifuc
orator
dateparser
lxml<5
hfutils[rar,7z]>=0.3.8
dghs-imgutils>=0.5.0
pyquery
markdownify<1
xmltodict
tzlocal
pixivpy3>=3.7.5
pyarrow
cryptography
pyyaml>=6
ao3_api
langdetect
pyanimeinfo>=0.0.4
pynacl
tqdm
hbutils
requests
httpx[http2]
# Cloudflare on donmai.us scores the TLS handshake itself, so neither credentials nor headers
# help. curl_cffi reproduces browser handshakes byte-for-byte, which is what gets the CDN to
# serve us; it must be a declared dependency rather than an inherited one.
curl_cffi
# Rust-backed LFS transfer. Upload throughput to the hub, not download, is what bounds a
# quick_dl run, so the accelerated path is worth having available.
hf_transfer
numpy
natsort
pytimeparse>=1.1.8
google-re2<1.1.20250722
pyrate-limiter<4