[pytest]
timeout = 300
# anyio comes in as a transitive dependency and registers a pytest plugin that imports
# _pytest.scope, which exists only from pytest 7 onwards while pytest here is pinned to 6.2.
# Collection then fails before any test runs. Nothing in this repo is async.
addopts = -p no:anyio
markers =
    unittest
    benchmark
    ignore
